<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
    <div class="container">
        <a class="navbar-brand fw-bold text-primary" href="/">
            <i class="fas fa-hospital-alt me-2"></i>Hospital Portal
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentPath != 'undefined' && currentPath == '/') ? 'active' : '' %>" href="/">Trang chủ</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentPath != 'undefined' && currentPath == '/doctors') ? 'active' : '' %>" href="/doctors">Đội ngũ bác sĩ</a>
                </li>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <li class="nav-item dropdown ms-3">
                        <a class="nav-link dropdown-toggle btn btn-outline-primary px-3 rounded-pill border-0" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle me-1"></i> <%= user.full_name %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow">
                            <li><a class="dropdown-item" href="/appointments"><i class="fas fa-history me-2"></i>Lịch sử khám</a></li>
                            <% if (user.role === 'admin') { %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/admin/dashboard"><i class="fas fa-user-shield me-2"></i>Trang quản trị</a></li>
                            <% } %>
                            <% if (user.role === 'doctor') { %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/doctor/dashboard"><i class="fas fa-stethoscope me-2"></i>Bàn làm việc Bác sĩ</a></li>
                            <% } %>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="#" id="logoutBtn"><i class="fas fa-sign-out-alt me-2"></i>Đăng xuất</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item ms-3">
                        <a class="btn btn-primary px-4 rounded-pill" href="/login">Đăng nhập</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>